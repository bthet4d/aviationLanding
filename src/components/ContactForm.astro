---
if (Astro.request.method === "POST") {
  try {
    const formData = await Astro.request.formData();
    console.log("Form Data:");
    console.log(formData);
    const name = formData.get("firstName");
    const lastName = formData.get("lastName");
    const email = formData.get("email");
    const interests = formData.getAll("interests");
    console.log("Name: ", name);
    console.log("Last Name: ", lastName);
    console.log("Email: ", email);
    console.log("interssts: ", interests);
    //TODO - PROCESS DATA
    
  } catch (error) {
    if (error instanceof Error) {
      console.log("Error:")
      console.error(error.message);
    }
  }

}
---

<div class="flex flex-col justify-center items-center mb-8 text-accent-c bold">
    <div class="m-8">
      <h2 class="text-3xl block float-right">Contact Us</h2>
      <br>
      <form id="contactForm" class="mt-4 txt-xl" method="POST">
        <div class="mb-4">
          <label for="firstName" class="block w-1/2 text-start">First Name</label>
          <input type="text" id="firstName" name="firstName" required class="w-full p-2 bg-gray-50 border border-gray-300 rounded-lg text-black">
        </div>
        <div class="mb-4">
          <label for="lastName" class="block w-1/2 text-start">Last Name</label>
          <input type="text" id="lastName" name="lastName" required class="w-full p-2 bg-gray-50 border border-gray-300 rounded-lg text-black">
        </div>
        <div class="mb-4">
          <label for="email" class="block text-start">Email</label>
          <input type="email" id="email" name="email" required class="w-full p-2 bg-gray-50 border border-gray-300 rounded-lg text-black">
        </div>

        <div class="mb-4">
          <label for="interests" class="block text-start">What interests you?</label>

          <select name="interests" id="interests" multiple class="h-auto bg-gray-50 border border-gray-300 text-black rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500">
            <option value="Content focused on the health of pets">Content focused on the health of pets</option>
            <option value="Content by trainers and behavior counselors">Content by trainers and behavior counselors</option>
            <option value="Content focused on food for pets">Content focused on food for pets</option>
            <option value="Content about various pet and animal rescue centers">Content about various pet and animal rescue centers</option>
            <option value="Content about puppies, kittens, and new pets">Content about puppies, kittens, and new pets</option>
            <option value="Content about wildlife and the animal kingdom">Content about wildlife and the animal kingdom</option>
            <option value="Content by pet groomers">Content by pet groomers</option>
            <option value="Content focusing on pets who are in their senior years">Content focusing on pets who are in their senior years</option>
            <option value="Content about traveling with pets">Content about traveling with pets</option>
            <option value="Content about service pets and service animals">Content about service pets and service animals</option>
            <option value="Movies about cats and dogs">Movies about cats and dogs</option>
          </select>
        </div>
        <button type="submit" class="text-white text-xl bold px-4 py-2 mt-2 rounded-full bg-accent-c">Submit</button>
      </form>
    </div>
  </div>
  